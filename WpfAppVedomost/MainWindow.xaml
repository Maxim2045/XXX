<Window x:Class="WpfAppVedomost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppVedomost"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="80" />
            <Setter Property="Margin" Value="5 6 5 6" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <RichTextBox x:Name="docBox" Margin="0,0,91.6,0" />
        <StackPanel Margin="302,0,0,0" HorizontalAlignment="Right" >
            <Button Content="Сохранить" Click="Save_Click"  />
            <Button Content="Загрузить"  Click="Load_Click" />
            <Button Content="Редактировать"  Click="Edit_Click" />
            <Button Content="Печать" />
            <Button Content="Авторизация" Click="Login_Click" />
        </StackPanel>
    </Grid>
</Window>